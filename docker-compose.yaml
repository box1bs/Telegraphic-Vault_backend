version: '3.8'
services:
  postgres:
    image: postgres:latest
    container_name: cash_db
    restart: always
    environment:
      POSTGRES_DB: cash
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "4500:5432"